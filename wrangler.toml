name = "nownow-tours"
compatibility_date = "2024-01-01"

# D1 Database configuration
[[d1_databases]]
binding = "DB"
database_name = "nownow-tours-db"
database_id = "bf32ad51-3976-4ee5-9940-d245999f170b"

# Environment variables
[vars]
ENVIRONMENT = "production"
# OPENAI_API_KEY will be provided via Wrangler secrets; do not hardcode secrets here.
 
# Pages build output directory (used by `wrangler pages deploy`)
pages_build_output_dir = "dist"